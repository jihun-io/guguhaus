name: Build & Deploy Frontend

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: jihun-io/guguhaus-frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Update k8s manifest in guguhaus-k8s
        uses: actions/checkout@v4
        with:
          repository: jihun-io/guguhaus-k8s
          token: ${{ secrets.K8S_REPO_TOKEN }}
          path: guguhaus-k8s

      - name: Patch image tag and push
        run: |
          sed -i "s|image: .*guguhaus-frontend.*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|" guguhaus-k8s/frontend/deployment.yaml
          cd guguhaus-k8s
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add frontend/deployment.yaml
          git commit -m "chore: update frontend image to ${{ github.sha }}"
          git push
